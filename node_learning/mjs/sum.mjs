// debemos exportar la funcion para hacer uso de esta funciona fuera del archivo
// sistema modrno de modulos (recomendado) => EcmaScriptModules (export - function)

export function sum(a, b) {
  return a + b;
}
